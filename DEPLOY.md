# Инструкция по развертыванию бота на бесплатных серверах

## 🚀 Варианты бесплатного хостинга

### 1. Railway.app (Рекомендуется) ⭐

**Преимущества:**
- 500 часов бесплатного времени в месяц
- Простое развертывание через GitHub
- Автоматический деплой при пуше в репозиторий
- Бесплатный SSL сертификат

**Инструкция:**

1. **Подготовка репозитория:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. **Создайте репозиторий на GitHub:**
   - Перейдите на https://github.com
   - Создайте новый репозиторий
   - Загрузите код:
   ```bash
   git remote add origin https://github.com/ваш-username/telegram_bot.git
   git push -u origin main
   ```

3. **Развертывание на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub
   - Нажмите "New Project" → "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит Python проект

4. **Настройка переменных окружения:**
   - В проекте Railway откройте вкладку "Variables"
   - Добавьте переменную: `BOT_TOKEN` = ваш токен бота
   - Railway автоматически перезапустит бота

5. **Готово!** Бот будет работать 24/7

---

### 2. Render.com

**Преимущества:**
- Бесплатный тариф с ограничениями (спит после 15 минут бездействия)
- Простое развертывание
- Автоматический деплой из GitHub

**Инструкция:**

1. **Подготовьте репозиторий GitHub** (как в Railway)

2. **Создайте файл `render.yaml`** (уже создан):
   ```yaml
   services:
     - type: worker
       name: telegram-bot
       env: python
       buildCommand: pip install -r requirements.txt
       startCommand: python bot.py
       envVars:
         - key: BOT_TOKEN
           sync: false
   ```

3. **Развертывание:**
   - Перейдите на https://render.com
   - Войдите через GitHub
   - Нажмите "New +" → "Blueprint"
   - Подключите репозиторий
   - Render автоматически использует `render.yaml`

4. **Настройка переменных:**
   - В настройках сервиса найдите "Environment"
   - Добавьте `BOT_TOKEN` = ваш токен

**⚠️ Важно:** На бесплатном тарифе Render бот будет "засыпать" после 15 минут бездействия и просыпаться при первом запросе (может быть задержка до 1 минуты).

---

### 3. PythonAnywhere

**Преимущества:**
- Полностью бесплатный тариф
- Бот работает постоянно
- Простой интерфейс

**Инструкция:**

1. **Регистрация:**
   - Перейдите на https://www.pythonanywhere.com
   - Зарегистрируйтесь (бесплатный аккаунт)

2. **Загрузка файлов:**
   - Откройте вкладку "Files"
   - Создайте папку для бота (например, `telegram_bot`)
   - Загрузите все файлы проекта через веб-интерфейс или используйте Bash консоль:
   ```bash
   git clone https://github.com/ваш-username/telegram_bot.git
   ```

3. **Установка зависимостей:**
   - Откройте вкладку "Tasks"
   - Создайте новую задачу:
   ```bash
   cd telegram_bot
   pip3.10 install --user -r requirements.txt
   ```

4. **Настройка переменных окружения:**
   - Откройте вкладку "Tasks"
   - Создайте файл `.env` или установите переменную:
   ```bash
   export BOT_TOKEN="ваш_токен"
   ```

5. **Запуск бота:**
   - Откройте вкладку "Tasks"
   - Создайте новую задачу:
   ```bash
   cd telegram_bot
   python3.10 bot.py
   ```
   - Или используйте "Always-on task" для постоянной работы

---

### 4. Fly.io

**Преимущества:**
- Бесплатный тариф с ограничениями
- Быстрое развертывание
- Хорошая документация

**Инструкция:**

1. **Установите Fly CLI:**
   ```bash
   # Windows (PowerShell)
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Создайте файл `fly.toml`** (уже создан):
   ```toml
   app = "your-bot-name"
   primary_region = "iad"

   [build]

   [env]
     BOT_TOKEN = "your-token-here"

   [[services]]
     internal_port = 8080
     protocol = "tcp"
   ```

3. **Развертывание:**
   ```bash
   fly auth login
   fly launch
   fly secrets set BOT_TOKEN="ваш_токен"
   fly deploy
   ```

---

## 📝 Общие рекомендации

### Безопасность:
- ✅ **НИКОГДА** не коммитьте `.env` файл в Git
- ✅ Используйте переменные окружения на сервере
- ✅ Добавьте `.env` в `.gitignore`

### Мониторинг:
- Настройте логирование для отслеживания ошибок
- Проверяйте статус бота периодически

### Резервное копирование:
- Храните код в GitHub
- Делайте регулярные коммиты

---

## 🎯 Рекомендация

**Для начала используйте Railway.app** - это самый простой и надежный вариант:
- ✅ Простое развертывание
- ✅ Автоматический деплой
- ✅ Бот работает постоянно
- ✅ Бесплатный тариф достаточен для личного использования

---

## ❓ Решение проблем

### Бот не запускается:
1. Проверьте логи на сервере
2. Убедитесь, что `BOT_TOKEN` установлен правильно
3. Проверьте, что все зависимости установлены

### Бот падает:
1. Проверьте логи на наличие ошибок
2. Убедитесь, что используете правильную версию Python
3. Проверьте ограничения бесплатного тарифа

### Вопросы?
Создайте Issue в репозитории или обратитесь к документации платформы.

